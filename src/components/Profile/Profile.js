import './Profile.css';
import Header from 'components/common/Header/Header';

function Profile() {
  return (
    <div>
      <Header isMainPage={false} />
      Profile
    </div>
  );
}

export default Profile;
